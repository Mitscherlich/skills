# {{id}} · {{title}} · spec

> **阶段：spec（intent 编译产物）**。本文件是 **actionable 的需求与设计规格**——过程性记录（问答、轮次、未决问题、裁决历史）已在编译时消除，只保留结论。
> 上游：`intent.md`
> intent_sha256: {{gate intent 通过时输出的 sha256}}
> 准出门禁：`devloop gate spec --file <本文件> --intent <intent.md>` 全绿 **且** 签署记录为「通过」，才能编译 plan.md。

## 目标与验收终态

{{一段话：交付后可被验证的终态。必须可被 R 表逐条对上}}

## 需求

> 每条需求一个稳定 ID `R<n>`，被 plan.md 的需求追溯表引用。四列都不能空。

| # | 需求 | 验收标准（可执行 / 可 grep / 可跑命令） | 来源 |
|---|---|---|---|
| R1 | {{要做什么}} | {{怎么证明做到了}} | {{I-1}} |

## 设计

### 方案

{{选定方案的一段话描述 + 为什么是它}}

### 组件与边界

| 组件 | 职责 | 不做什么 |
|---|---|---|
| {{组件}} | {{职责}} | {{显式排除的职责}} |

### 数据 / 接口契约

{{新增或变更的数据结构、接口、配置项、迁移。没有则写「无」}}

## 关注点与冲突

> 参考 AI-native SDLC playbook：**明确写出无法同时满足的策略冲突**，不要藏。每条给出取舍建议与需要谁拍板。

| # | 关注点 / 冲突 | 影响 | 建议取舍 | 需谁拍板 |
|---|---|---|---|---|
| C1 | {{冲突描述，或写「无」}} | {{影响面}} | {{建议}} | {{角色}} |

## 非目标

- {{本 spec 明确不覆盖的范围}}

## 签署记录

- 签署人：{{产品 / 技术负责人}}
- 签署时间：{{ISO 时间}}
- 签署范围：{{本 spec 覆盖哪些 R，哪些留待后续}}
- 结论：{{通过 —— 或写「打回」并说明，然后回到 intent / spec 修订}}
